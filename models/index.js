const Categoria = require('./categoria');
const Role = require('./role');
const Server = require('./server');
const Producto = require('./producto');
const Usuarios = require('./usuario');
const ChatMensajes = require('./chat-mensaje');

module.exports = {
    Categoria,
    Producto,
    Role,
    Server,
    Usuarios,
    ChatMensajes,
}